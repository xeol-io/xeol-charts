apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xeol-agent.fullname" . }}-test-connection"
  labels:
    {{- include "xeol-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xeol-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
